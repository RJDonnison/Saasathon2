JWT_SECRET=
# required — Supabase is the database (Project Settings -> API). Keep the service-role key server-side only.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# not used by any route yet (AI hints are mocked)
OPENAI_API_KEY=
