// PLACEHOLDER: static content. Real version loads the module via api.getModule().
export default function ModuleView() {
  return (
    <section className="rounded border bg-white p-4">
      <h2 className="mb-2 font-semibold">Module: Variables and Types</h2>
      <p className="text-sm text-gray-600">
        Declare a variable with <code>let</code> or <code>const</code>. Try printing the sum of two numbers.
      </p>
    </section>
  )
}
